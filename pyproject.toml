[project]
name = "ptcg-agent"
version = "0.1.0"
description = "Pokemon TCG AI Battle Challenge - RL Agent"
requires-python = ">=3.12"
dependencies = [
    "kaggle-environments",
    "torch",
    "numpy",
    "pandas",
    "tqdm",
    "gymnasium",
    "tensorboard",
]
